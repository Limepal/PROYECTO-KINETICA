tener python version Python 3.11.9

Para poder correr el proyecto:

- camputere-camples(para poder recolectar imagenes)
- python normalize_samples.py
- python create_keypoints.py
- python training_model.py
- python evaluate_model.py (para probar el modelo)

recomendacion:
hacer minimo 7 muestras por cada palabra, de cada mano, si es una seña de una mano hacer de cada mano y poner el nombre con ese formato '{nombre_seña}-izq' o '{nombre_seña}-der', para poder insertar y tener el diccionairo completo de las palabras estan en el archivo words.jsn que esta en la carpeta de 'models'
